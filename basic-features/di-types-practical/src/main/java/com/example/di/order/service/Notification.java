package com.example.di.order.service;

/*
    Q. Can we mix and match @Order (SPRING) and @Priority (JSR-250)
    A. Yes, not recommended
    Q. Which has higher priority? @Order / @Priority? Why?
    A.

 */
public interface Notification {
}
